<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".layerListActivity">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/layer_img"/>

    <TextView
        android:layout_marginTop="30dp"
        android:layout_gravity="center"
        android:background="@drawable/layer_shadow"
        android:layout_width="300dp"
        android:layout_height="80dp"/>

    <RadioGroup
        android:layout_marginTop="20dp"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RadioButton
            android:text="篮球"
            style="@style/LayerRadioButtonStyle"
            />

        <RadioButton
            android:text="足球"
            style="@style/LayerRadioButtonStyle"
            />

        <RadioButton
            android:text="排球"
            style="@style/LayerRadioButtonStyle"
            />
        <RadioButton
            android:text="羽毛球"
            style="@style/LayerRadioButtonStyle"
            />

    </RadioGroup>


    <ProgressBar
        android:layout_margin="20dp"
        android:id="@+id/progress"
        android:progress="45"
        android:layout_below="@+id/tv_mAh"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:progressDrawable="@drawable/progress_bar_drawbale"
        android:layout_width="match_parent"
        android:layout_height="10dp"/>

    <Button
        android:text="开始执行"
        android:onClick="onLayerClick"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:ignore="OnClick" />
</LinearLayout>